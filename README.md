# ideavim config

## where i belong

`~/.ideavimrc`

## setup

```bash
# ensure u are in ur home dir ~
cd

# this should clone the repo into ~/.ideavim
git clone <this-repo>

# create a soft link
ln -s ~/.ideavim/ideavimrc ~/.ideavimrc
```
